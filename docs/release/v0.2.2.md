# CaracalDB v0.2.2 Release Notes

**Tag:** `v0.2.2`
**Date:** 2026-05-02
**Distribution:** `dist/caracaldb-0.2.2-py3-none-any.whl` + `dist/caracaldb-0.2.2.tar.gz`

## Highlights

- Updated package release metadata for the v0.2.2 patch line.
- Refreshed ML integration documentation to reference the current v0.2.2 resource ingest behavior.
- Kept generated docs and Quickstart-adjacent release checks aligned with the v0.2.x documentation surface.

## Build verification

```powershell
uv run --with hatch hatch build
# dist/caracaldb-0.2.2.tar.gz
# dist/caracaldb-0.2.2-py3-none-any.whl
```

## Smoke install

```powershell
uv pip install dist/caracaldb-0.2.2-py3-none-any.whl
python -c "import caracaldb as cdb; print(cdb.__version__)"  # 0.2.2
caracal --help
```

## Compatibility

This is a patch release. Existing 0.2.1 resource ingest, generated diagnostics, `connect`, `define_class`, `insert_nodes`, typed table ingest, and focused `db.sql()` query APIs remain available.
